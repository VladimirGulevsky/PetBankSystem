spring:
  jpa:
    database: postgresql
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: update
    properties:
  datasource:
    url: jdbc:postgresql://localhost:9999/bill_db
    platform: postgres
    username: root
    password: root
server:
  port: 8081